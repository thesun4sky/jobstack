{
  "name": "jobstack-bin",
  "version": "1.0.0",
  "description": "jobstack 스크립트 의존성 — 채용공고 수집(Playwright·cheerio), .docx 산출(docx), YAML 계약(yaml)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "docx": "^9.7.1",
    "playwright": "~1.62.1",
    "yaml": "^2.8.0"
  }
}
