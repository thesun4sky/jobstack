# bin/vendor

- `marked.umd.js` — marked 18.0.11 (MIT, https://github.com/markedjs/marked). `bin/jobstack-view`가 생성 HTML에 인라인해 오프라인에서도 본문을 렌더한다. 갱신: `npm pack marked@<ver>` 후 `package/lib/marked.umd.js` 교체.
