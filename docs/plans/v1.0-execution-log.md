# v0.4.0 → v1.0 실행 로그

- **근거 문서**: `docs/plans/version-upgrade-review-2026-09.md` (후보 U-01~U-23)
- **브랜치**: `claude/project-version-upgrade-review-poey5r` · **시작 커밋**: `ff5e9b9`
- **원칙**: 단계마다 구현 → 결정적 테스트 → 다각도 코드리뷰(저비용 모델 팬아웃) → 확인된 지적 수정 → 커밋. 시장 관련 스킬 본문 변경은 원문 확인 전까지 `[2차]` 근거를 그대로 옮기지 않는다.

## 모델 배분 정책

| 난이도 | 작업 | 모델 |
|---|---|---|
| 판단·설계 | 아키텍처 결정, 변환 규칙, 통합, 리뷰 지적 확정·수정 | Fable 5.1 (세션 모델) |
| 중간 | 거대 스킬 분할·참조 파일 작성, 스크립트 구현, 정확성·보안 리뷰 | Sonnet 5 서브에이전트 |
| 기계적 | 컨벤션·표기 점검, 목록 대조, 설명 문구 정리 | Haiku 4.5 서브에이전트 |

기계적 변환(16개 SKILL.md 헤드 교체·경로 치환·봇 블록 이관)은 LLM이 아니라 **결정적 스크립트**로 수행하고 테스트로 검증했다 — 같은 작업을 모델에 맡기는 것보다 싸고 재현된다.

---

## v0.4.0 — 결함 수정 (U-01~U-06)

### 구현

| 항목 | 내용 | 검증 |
|---|---|---|
| U-01 | `templates/`·`docs/` → 각 스킬 `references/` 복제(`bin/gen-skill-docs.sh`), 가드레일은 `!`sed … guardrails.md`` 로드 시점 인라인, 참조 113곳을 `${CLAUDE_SKILL_DIR}/references/…`·`$_JS_BIN/…`로 치환 | `test/test-skill-refs.sh`(경로 안쪽 강제·드리프트·봇 마커), gen `--check` |
| U-02 | `templates/bot-protocol.md` + `templates/bot/<skill>.md`(8개), 프리앰블 `JOBSTACK_RUNTIME` 판정, 봇 주입 라인 | 위 린트 ③④ |
| U-03 | `bin/jobstack-view`: textarea 이스케이프, marked 18.0.11 인라인(`bin/vendor/`), 폴백 | 통합 테스트 `</script>` 케이스 |
| U-04 | `bin/jobstack-preamble` + `templates/skill-preamble.sh`(로케이터, 실경로 탐색) + `env.sh` | `test/test-preambles.sh`(심링크 경로 주입, 봇 판정, stale 정리, 폴백) |
| U-05 | CHANGELOG 0.4.0(7월 소급), VERSION, README, CLAUDE.md, CONTRIBUTING, `.github/workflows/ci.yml` | CI |
| U-06 | retro WebSearch·defense-map, auto 라우팅 표, scout-profile 도구 축소, ncs 경험 카드, 변수명 통일, programmers 제거 | 린트·`node --check` |

### 실측 근거

- `!` 동적 주입이 `~/.claude/skills/` 심링크·복사 설치 모두에서 실행되고 `${CLAUDE_SKILL_DIR}`가 명령 안에서 치환됨을 nested `claude -p`로 확인(마커 출력 요청은 모델이 프롬프트 인젝션으로 거절할 수 있어, 검증 프롬프트는 정상 과업처럼 써야 한다).
- Read 도구는 심링크 `..` 경로를 열지 못하고 Bash는 여는 것을 재확인 → 규칙 "참조는 스킬 안쪽만".

### 리뷰

워크플로우 `review-v040`: 스테이징 diff 를 4관점으로 나눠 발견(scripts·skills·behavior: Sonnet 5, conventions: Haiku 4.5)하고 지적마다 Sonnet 5 검증자가 적대적으로 재현했다. 4관점 20건(중복 2건 포함)은 아래 표. 검증자 판정이 아직 안 나온 항목은 "검증 대기"로 두고 후속 커밋에서 갱신한다.

| # | 관점 | 대상 | 지적 | 판정 | 조치 |
|---|---|---|---|---|---|
| 1 | scripts | `bin/jobstack-preamble`, `templates/skill-preamble.sh` | `$HOME` 미설정이면 `set -u`로 죽어 "항상 exit 0" 계약 위반(3경로 재현) | 확인 high | `: "${HOME:=/tmp}"` 추가 |
| 2 | scripts | `templates/skill-preamble.sh` 폴백 | env.sh 를 직접 리다이렉트로 써 비원자적 | 확인 medium | tmp+mv 로 통일 |
| 3 | scripts | `bin/gen-skill-docs.sh`, `test/test-preambles.sh` | 템플릿 없는 `mktemp` 호출의 BSD 호환 | 확인 low(구형 BSD 한정) | `${TMPDIR:-/tmp}/…XXXXXX` 템플릿 명시(무해) |
| 4 | scripts | `bin/jobstack-preamble` | 상태 트리·env.sh 가 umask 022 로 생성돼 다른 로컬 계정이 읽음(PII) | 적용 후 검증자 재현 불가(=수정 반영 확인) | `umask 077`, 새 상태 디렉토리 700 |
| 5 | scripts | `bin/jobstack-view` | 새 vendor/cdn 변수가 셸→파이썬 따옴표 삽입 패턴 반복 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 모든 셸 값을 환경변수로 전달(INPUT/OUTPUT 포함) |
| 6 | scripts | `install.sh` | untracked `agents/researcher.md` 가 심링크 로직에 걸림(위생) | — | v0.4.0 커밋에서 제외, v0.5.0 에서 정식 추가 |
| 7 | skills | `company-research/SKILL.md` | 일괄 치환으로 "안전/위험" 대조문이 같은 변수를 가리키는 자기모순 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 대조 의도로 문구 복원 |
| 8 | skills | `portfolio/SKILL.md` | env.sh 소싱 없이 프로즈에 `$_JS_BIN` 노출 | 확인 medium | 펜스+env.sh 첫 줄로 통일 |
| 9 | skills | `salary/SKILL.md` 4곳 | 프리앰블에서 사라진 `CURRENT_YEAR` 참조 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 프리앰블 출력·env.sh 에 `CURRENT_YEAR` 복원 |
| 10 | skills | `auto/SKILL.md` 라우팅 표 | U-06 이 요구한 ncs 목적지 누락 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 행 추가(CLI: ncs 스킬, 봇: `/cover_letter` 보강) |
| 11 | skills | `resume/SKILL.md` | 이관 후 빈 `## 프리앰블` 헤딩 잔존 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 삭제 |
| 12 | skills | 16개 SKILL.md 상단 | 폴백 감지 문자열이 실측 근거 없음 | 적용 후 검증자 재현 불가(=수정 반영 확인) | "KEY=VALUE 목록 대신 `!` 명령·차단 문구가 보이면"으로 완화 |
| 13 | skills | `tracker/SKILL.md` | 구 프리앰블의 파일 사전 생성·건수·최근 5건 미리보기 유실 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 프리앰블에 tracker 확장(TRACKER_FILE·ENTRY_COUNT·최근 5건) |
| 14 | conventions | `CHANGELOG.md` | IMAGE_PROMPT 스킬 수 6→5 | 적용 후 검증자 재현 불가(=수정 반영 확인) | 수정 |
| 15 | behavior | retro·tracker·job-search·mock-interview·cover-letter·resume | bare `docs/…md`·`templates/…md` 참조 14곳이 남아 심링크·복사 설치에서 Read 실패(D-1 회귀) | 자체 재현 high | `${CLAUDE_SKILL_DIR}/references/`로 치환, gen-skill-docs 가 bare `references/` 언급도 복제, `test-skill-refs` ⑤ 신설 |
| 16 | behavior | `bin/jobstack-preamble` | 상태 디렉토리·env.sh 쓰기 실패를 삼키고 정상 컨텍스트를 출력해 폴백 조건에 안 걸림 | 자체 재현 high | `STATE_WRITE_FAILED` 마커·경고 줄, 폴백 문구에 조건 추가, `test-preambles` (f) |
| 17 | behavior | `auto/SKILL.md` | ncs 라우팅 없음 | #10 과 중복 | #10 |
| 18 | behavior | `career-history/SKILL.md` Phase 6 | 문서 스킬 4개 중 유일하게 봇 런타임 분기 문구 없음 | 확인 medium | 분기 문구 추가(뷰어·docx 안내는 CLI 전용) |
| 19 | behavior | 16개 SKILL.md 상단 | Cowork 를 정책 차단과 같은 폴백으로 묶어 미검증 동작을 단정 | 확인 medium | 문장 분리 — 정책 차단은 Bash 재실행, 스킬 디렉토리가 없는 환경은 상태 저장·스크립트 단계 생략 |
| 20 | behavior | `resume/SKILL.md` | 고아 헤딩 | #11 과 중복 | #11 |

수정은 격리 복사본에서 먼저 적용해 lint 6종·`test-preambles` 17/17·통합 74/74·Node 4종을 통과시킨 뒤 작업 트리에 옮겼다. 검증 팬아웃(24 에이전트, 약 79분)은 수정 적용·커밋 d9ac8c7 이후에 끝나, 판정은 확인 4건(#1 high·#2 medium·#3 low·#8 medium)과 "현재 트리에서 재현 불가" 16건으로 나왔다 — 후자는 수정이 반영됐음을 검증자가 독립적으로 확인한 결과로 기록한다(예: umask 077·chmod 700 존재, CURRENT_YEAR 제공, bare 참조 0건).

---

## v0.5.0 — 실행 기반 현대화 (U-07~U-13, U-17, U-21)

### 구현

| 항목 | 내용 | 담당 모델 | 검증 |
|---|---|---|---|
| U-07 | `.claude-plugin/plugin.json`·`marketplace.json`, 플러그인 데이터 디렉토리 node_modules 심링크(프리앰블 3번째 인자) | Fable 5.1(설계) · Haiku 4.5(매니페스트 테스트) | `test-plugin-manifest`, `test-preambles` (g), `claude plugin validate` |
| U-08 | 6개 스킬 분할 — Sonnet 5 에이전트 6개가 스크래치에 산출, 결정적 검증기(`split_check.py`: 300줄·프론트매터·주입 블록 바이트 동일·원본 줄 보존·상호 참조·펜스)로 [PASS] 후 적용. 나머지 10개 프론트매터는 Haiku 4.5 | Sonnet 5 · Haiku 4.5 | `test-skill-size --frontmatter`, `test-skill-refs`, 원본 줄 유실 0 |
| U-08 실측 | 줄 수: auto 338→275, cover-letter 505→294, resume 487→296, mock-interview 461→292, job-search 443→264, company-research 419→260. 바이트(v0.5.0 커밋 기준 재측정): auto 8%(hwpx·라우팅 행 추가분 포함)·cover-letter 38%·resume 39%·mock-interview 28%·job-search 27%·company-research 34% 감소, 평균 약 29% — 수용 기준 "초기 로드 50% 감소"는 미달. 게이트·판단 분기를 SKILL.md에 남기는 규칙을 우선했고, 더 줄이려면 판단 규칙까지 references 로 밀어야 해 채택하지 않았다 | — | 300줄 상한은 충족 |
| U-09 | `jobstack-tracker`(Fable 초안, 테스트 35), `jobstack-exp.mjs`(59)·`jobstack-defense-map.mjs`(72), `jobstack-ats-match`·`jobstack-retro-stats`(+테스트), tracker·retro·experience-bank·ncs 배선 | Sonnet 5(구현·배선) | 각 테스트, `test-script-layer` |
| U-10 | `agents/researcher.md` + company-research·salary·strategy fan-out(Agent 도구) | Fable 5.1 | 배선 리뷰 |
| U-11 | `bin/hwpx2md.py`(테스트 10) + auto Phase 1 | Fable 5.1 | `test-hwpx` |
| U-12 | is-fetch 경계 마커·block_class·Retry-After·--profiles(테스트 46), `jobstack-fetch-diag`(39), saramin 파서(13)·API(10)·fetch-jobs lazy 브라우저 | Sonnet 5 | 각 테스트 + 어댑터 회귀 |
| U-13 | `md2docx.mjs`·`jobstack-export` 폴백(테스트 23+23) | Sonnet 5 | `test-export`, `test-md2docx` |
| U-17 | `docs/ncs-competencies.md` → references, 하드코딩 목록 제거 | Fable 5.1 · Sonnet 5(ncs 배선) | `grep` 0건, 컨벤션 린트 |
| U-21 | `templates/structured-modes.md`, Phase 0 D, `ai_usage` | Sonnet 5(초안) · Fable 5.1(통합) | 컨벤션 린트 |

병렬 실행: 스크립트 구현 4개·분할 6개·테스트 2개·배선 3개 에이전트를 파일 집합이 겹치지 않게 나눠 동시에 돌렸고, 스테이징된 v0.4.0 파일은 커밋 전까지 건드리지 않았다.

### 리뷰

워크플로우 `review-v050`: v0.5.0 커밋(648f5d7)을 체크아웃한 읽기 전용 worktree 를 5관점(scripts·split·wiring·headless: Sonnet 5, conventions: Haiku 4.5)에서 발견하고 지적마다 Sonnet 5 검증자가 적대적으로 재현했다. headless 관점은 격리 HOME 에 심링크 설치한 뒤 `claude -p --model sonnet --max-turns 6 --permission-mode acceptEdits` 로 /auto·/tracker·/experience_bank·/resume 를 실제 실행해 관찰했다. 1차 팬아웃은 Workflow 의 worktree 격리가 에이전트 일부를 옛 main(a5ef5ed)에 두어 가짜 회귀를 보고해 폐기했고, 고정 worktree 를 직접 만들어 재실행했다. 5관점 32건(중복 4건)은 아래 표. 수정은 검증 팬아웃이 끝나기 전에 직접 재현·대조하며 먼저 적용했고, 판정 열은 이후 도착한 검증자 판정으로 전부 갱신했다.

| # | 관점 | 대상 | 지적 | 판정 | 조치 |
|---|---|---|---|---|---|
| 1 | scripts | `bin/hwpx2md.py` | zip 엔트리 크기 상한 없음 — 51KB 압축폭탄으로 RSS 113MB 재현 | 검증자 확인 high | 엔트리 20MB·아카이브 50MB 상한, 스트리밍 해제 중 초과 시 exit 2 |
| 2 | scripts | `bin/is-fetch.py` | Retry-After 를 `isdigit()` 로 검사한 뒤 `int()` — `'5²'` 로 크래시 | 검증자 확인 medium(원 high) | ASCII 십진수 정규식 + try/except |
| 3 | scripts | `bin/parsers/saramin.mjs` | cheerio 최상위 require 로 jumpit 등 다른 플랫폼 수집까지 크래시 | 검증자 확인 high | 파서 호출 시점 동적 import |
| 4 | scripts | `bin/hwpx2md.py` | 중첩 표의 안쪽 행이 바깥 표에 중복 삽입 | 검증자 확인 medium | 직계 `tr` 만 순회 |
| 5 | scripts | `bin/parsers/saramin.mjs` | `.text()` 가 `<script>` JSON-LD 를 포함해 마감일 오검출 | 검증자 확인 medium | script/style 제거 후 추출, 파서 테스트 카드 추가 |
| 6 | scripts | `bin/jobstack-tracker` | 손상 JSONL 에 `validate` 는 exit 2, 나머지 서브커맨드는 exit 1 | 검증자 확인 medium | 파싱 실패는 항상 exit 2 |
| 7 | scripts | `bin/jobstack-exp.mjs` | `update` 가 ai_usage 플래그 일부만 받아 스키마 위반 카드를 저장(#30 과 중복) | 검증자 기각 — 원 설계는 하나씩 채우기를 허용하고 `validate` 가 게이트(스키마 문서·test-exp 가 명시) | 라운드 2 에서 이미 "갱신 후 세 값이 모두 차야 저장"(완전한 ai_usage 가 있으면 일부 갱신 허용)으로 바꿨으므로 스키마 문서를 새 동작에 맞춰 갱신 — 불완전한 ai_usage 가 저장되는 경로를 두지 않는 쪽을 택함 |
| 8 | scripts | `bin/hwpx2md.py` | 깊게 중첩된 XML 의 RecursionError 가 traceback 으로 노출 | 검증자 확인 medium | except 에 추가 → "[오류] 파싱 실패" exit 2 |
| 9 | scripts | `jobstack-ats-match`·`jobstack-tracker`·`hwpx2md.py` | argparse 인자 오류의 exit 2 가 종료 코드 계약 밖 | 검증자 확인 low | docstring 에 명시 |
| 10 | scripts | `bin/jobstack-fetch-diag` | URL 리댁션 실패 시 원문 반환(fail-open) | 검증자 확인 low | 고정 마커 반환(fail-closed) |
| 11 | scripts | `bin/md2docx.mjs` | 최종 .docx 를 직접 씀(비원자적) | 검증자 확인 low | tmp+rename |
| 12 | split | `resume/SKILL.md` | 신입/중고신입/경력 판별 지시가 없어 tracks/*.md 게이트가 안 걸림 | 검증자 확인 medium(원 high) | Phase 1 에 "지원 유형 확인(모든 모드)" 추가 |
| 13 | split | `templates/structured-modes.md` §4 | 구조화 전형 완료 게이트가 SKILL.md 완료 상태 절과 미연결 | 검증자 확인 medium | 완료 상태 절에 §4 역참조 |
| 14 | split | `cover-letter/SKILL.md` Phase 8 | defense-map 포인터에 Read 시점이 없고 필수 필드 누락 | 검증자 확인 medium | "entries-json 구성 직전 Read" + 필드 전체 열거 |
| 15 | split | `job-search/SKILL.md` | argument-hint 의 `--platform` 이 본문에 미구현 | 검증자 확인 medium | Phase 2 에 단일 플랫폼 수집 분기 |
| 16 | split | `company-research/SKILL.md` | argument-hint 의 `--deep` 미구현 | 검증자 확인 medium | 힌트에서 제거 |
| 17 | split | `auto/SKILL.md` 라우팅 표 | NCS 행 중복(#25·#29 와 중복) | 검증자 확인 low | 삭제 |
| 18 | split | `templates/defense-map-schema.md` | "미끼 5개 배치 원칙" 이 cover-letter 어디에도 없음 | 검증자 확인 medium(원 low) | cover-letter Phase 8 에 "미끼 포인트 최소 5개" 명문화 |
| 19 | split | `cover-letter/references/review-steps.md` | 과정→결과 예시가 experience-methods §6 과 다른 수치 | 검증자 확인 low | §6 예시로 통일 |
| 20 | wiring | `docs/experience-card-schema.md` | resume·mock-interview 소비 계약이 실제로 미배선 | 검증자 확인 high | resume Phase 2A·mock-interview 자동 로드에 `jobstack-exp.mjs list/show` |
| 21 | wiring | `tracker/SKILL.md` | 정수 전용 `--fit-score` 에 넘길 "종합 적합도" 가 company-research 산출물에 없음 | 검증자 확인 high | fit-scoring 에 종합 적합도(정수, 세 점수 평균 반올림) 정의·캐시 요약에 기록, tracker 는 그 정수만 전달 |
| 22 | wiring | `review/SKILL.md` | "heredoc 으로 기록" 과 "스크립트로만 저장" 이 한 문장에 공존 | 검증자 확인 medium | heredoc 구절 삭제 |
| 23 | wiring | `agents/researcher.md` | `partial: true` 신호를 세 스킬이 소비하지 않음 | 검증자 확인 medium | 반환 형식에 partial 추가, 세 스킬 병렬 리서치 문단에 partial 처리 |
| 24 | wiring | `docs/ncs-competencies.md` | 헤더가 resume(블라인드 체크) 소비를 주장하나 미배선 | 검증자 확인 low | 소비자 목록에서 resume 제외 |
| 25 | wiring | `auto/SKILL.md` | NCS 행 중복 | #17 과 중복(검증자 확인 low) | #17 |
| 26 | conventions | `CHANGELOG.md` 0.5.0 | 바이트 감소율 "26~38%" 가 실측(8~39%)과 다름 | 검증자 확인 low(원 medium) | "8~39%, 평균 약 29%" 로 정정 |
| 27 | headless | company-research·salary·strategy allowed-tools | `Agent` 가 헤드리스 init 이벤트 도구 목록에 없고 `Task` 만 있음 | 검증자 기각 — 관찰(init 의 tools 배열은 `Task`)은 재현되나, `Agent` 로 선언한 스킬에서 researcher 서브에이전트 호출이 `--permission-prompts none` 아래서도 막히지 않음을 실행으로 반증(공식 permissions·sub-agents 문서 표기도 `Agent`) | 두 이름을 함께 선언(무해)·`test-skill-size --frontmatter` 도구명 검사는 오타 방지용으로 유지 |
| 28 | headless | `auto/SKILL.md` Phase 2 | 예시 PII(홍길동·example.com)를 확인 없이 프로필에 확정 저장(실행 재현, resume 는 먼저 확인함) | 검증자 확인 medium(원 high, 실행 재현) | 추출 가드레일 6번(resume #130 과 같은 규칙, 확인 전 placeholder) |
| 29 | headless | `auto/SKILL.md` | NCS 행 중복 | #17 과 중복(검증자 확인 low, 원 medium) | #17 |
| 30 | headless | `bin/jobstack-exp.mjs` | update ai_usage 비대칭 | #7 과 중복(검증자 기각) | #7 |
| 31 | headless | `auto/SKILL.md` Phase 1 | Glob 지시를 `ls -la` 로 대체(실행 재현) | 검증자 확인 low(2/2 재현, 실제 호출은 `ls -la` + `find -maxdepth 3`) — 이 컨테이너의 헤드리스 도구 목록에 Glob 자체가 없어 환경 요인 | "Glob 없으면 `find`" 대체 경로 명시 |
| 32 | headless | `templates/structured-modes.md` | 글자수 스크립트가 스페이스만 제외 | 검증자 확인 low | 개행·탭·전각공백 포함 값 병기, 폼 카운터 우선 |

헤드리스 스모크 관찰(사실만): /auto 는 프리앰블 KEY=VALUE 블록과 guardrails 본문이 첫 프롬프트에 주입됐고 dashboard·cases 참조를 해당 Phase 직전에만 Read 했으며, 프로필 추출은 날조 없이 정확했고 중고신입 산술(인턴 6개월만 합산, 근거 병기)도 맞았다. /tracker add 는 Write/Edit 없이 `jobstack-tracker nudge --mark`·`add` 만 호출했다. /experience_bank 는 `jobstack-exp.mjs add` 후 `validate` [PASS] 까지 진행했고 AI 미사용이면 세 플래그를 통째로 생략했다. /resume 첨삭은 references 10개 중 diagnosis·korean-format 2개만 읽어 진행적 공개가 실제로 작동했다. 실행 제약: root 컨테이너에서 `--dangerously-skip-permissions` 는 거부되어 `--permission-mode acceptEdits` 를 썼고, 동시 실행 4건 중 2건이 상위 샌드박스 분류기에 막혀 재시도했다. wiring 관점 스모크: worktree 에서 통합 테스트 90/90, `gen-skill-docs.sh --check` 드리프트 0, 플러그인 매니페스트·install.sh SKILL_DIRS 16개 일치, 프리앰블 로케이터가 심링크·플러그인 경로 양쪽에서 bin 을 찾음.

검증 팬아웃 완료(검증자 32, 총 37 에이전트·약 133분): real 29·기각 3 — #7·#30 은 같은 지적(하나씩 채우기가 문서화된 설계라는 판정; 라운드 2 의 제약은 유지하고 문서를 맞춤), #27 은 실행 테스트로 영향 반증. 등급 조정은 high→medium 3건(#2·#12·#28), low→medium 1건(#18), medium→low 2건(#26·#29).


## v1.0 — 기능 확장 (U-14~U-16, U-18~U-20, U-22, U-23)

### 구현

| 항목 | 내용 | 담당 모델 | 검증 |
|---|---|---|---|
| U-14 | `bin/jobstack-cron`(run/install/uninstall/status, 새 공고 diff·D-7·nudge, cron/launchd), `docs/routines.md`, job-search 안내 | Sonnet 5(초안·테스트 32) · Fable 5.1(통합) | `test-cron.sh` |
| U-15 | `templates/chrome-path.md` → scout-profile·company-research·job-search references, 읽기 전용 원칙 | Fable 5.1 | 컨벤션 린트, skill-refs |
| U-16 | `evals/<skill>/evals.json` 15케이스 + 트리거 질의, `test/run-evals.sh`(stream-json 파싱, gate/periodic/e2e), `docs/evals.md` | Sonnet 5(초안) · Fable 5.1(통합·실행) | `--dry-run` 15케이스 나열, gate 실측(아래) |
| U-18 | `bin/package-skill.sh`, `docs/cowork.md`, 패키지 단독 실행 폴백 검증 | Fable 5.1 | `test-package-skill.sh` 10 |
| U-19 | tracker `model: sonnet`·`effort: low`, 문서·면접·기업분석·전략 7개 `effort: high` | Fable 5.1 | 프론트매터 린트, 비교 실측(아래) |
| U-20 | `bin/jobstack-learn`(PII 휴리스틱·dup·top), `docs/telemetry-events.md` 어휘 등록 후 auto 대시보드·job-search·company-research 배선 | Sonnet 5(초안·테스트 76) · Fable 5.1(통합) | `test-learn.sh` |
| U-22 | mock-interview E-2 서브모드, company-research 전형 확인 ⑥ | Fable 5.1 | 컨벤션 린트 |
| U-23 | `templates/policy-checklist.md` → tracker·salary·strategy references(수치 없음, 실행 시 확인) | Fable 5.1 | `lint-conventions` 시장 수치 검사 |

### 실측·미검증 항목

- U-14: 네트워크 없는 결정적 테스트(가짜 fetch-jobs)로 검증. 실제 플랫폼 수집은 이 환경의 egress 차단으로 실측하지 못함.
- U-18: 패키지 구성·폴백 컨텍스트는 검증했으나 실제 Cowork 세션 실행은 미검증(`docs/cowork.md` 확인 항목).
- U-16 실측(헤드리스 `claude -p --permission-mode auto`, 격리 HOME, 2026-09-03): 1차 실행 6/15 통과(gate 3/5·periodic 2/5·e2e 1/5). 실패 원인은 세 갈래 — ① runner 결함 2건(권한 플래그가 "전부 거부"였음, `while read` 루프 안 `claude -p` 가 표준입력을 삼켜 두 번째 케이스부터 누락) → 수정, ② 케이스 문구의 리터럴 취약(모델이 "예시값"→"예시/샘플값", 스크립트 출력을 요약해 "갱신됨" 소실) → 기대값 완화, ③ 스킬 결함 — cover-letter 가 작업 폴더의 자소서 파일을 찾지 않고 되물음(Phase 0 "입력 확보" 규칙 추가), resume 케이스 프롬프트가 CLI 인자 지시라 Skill 대신 셸 탐색으로 흘러 max_turns 초과(프롬프트를 `/resume …` 로 교정). 수정 후 gate 5/5, periodic 3/5, e2e 3/5 — 총 11/15(표는 `docs/E2E-TEST-REPORT.md` 상단). 남은 실패는 헤드리스 1턴에서 질문에 답할 사람이 없는 흐름 3건과 스킬 지시 미준수 1건(cover-letter 반영률 스크립트 미호출). tracker 의 "stats 출력 요약" 은 러너에 `must_output`(도구 결과 텍스트 판정)을 추가해 해결 — 도구 출력은 사용자 화면에 보이므로 프로즈 리터럴 요구가 과했다.
- U-19 실측(tracker gate 케이스, 각 2회): sonnet PASS·4턴·26s·$0.19/0.18 vs opus PASS·4턴·23~29s·$0.94/0.47 — 판정·턴 수 동일, opus 비용 2.5~5배·편차 큼 → tracker `model: sonnet`·`effort: low` 확정. resume gate 는 교정 전 프롬프트에서 두 모델 모두 같은 방식으로 실패(스킬 미호출) — 모델 차이가 아니라 프롬프트 문제. 문서 스킬의 `effort: high` 는 품질 근거를 이번 실측으로 분리해 내지 못해 검토 보고서의 권고(2-2)를 따른 기본값이다.
- 트리거 정확도(tracker, 10 질의): TP 4 · FP 0 · TN 6 · FN 0.
- 알려진 비결정성: tracker-periodic 케이스는 독립 2회 실행에서 PASS/FAIL 이 갈렸다(리터럴 "채용서류 반환" 유무) — periodic 계층은 게이트가 아니라 추세 관찰용으로 둔다.

### 리뷰

워크플로우 `review-v100`: v1.0 커밋(29d1847) worktree 를 4관점(scripts·wiring·claims: Sonnet 5, conventions: Haiku 4.5)에서 발견하고 지적마다 Sonnet 5 검증자가 격리 디렉토리에서 재현했다. claims 관점은 공식 문서(code.claude.com 의 routines·chrome·web·permissions)를 2026-09-03 에 직접 대조했다. 4관점 26건(중복 1건, conventions 0건)은 아래 표.

| # | 관점 | 대상 | 지적 | 판정 | 조치 |
|---|---|---|---|---|---|
| 1 | scripts | `bin/package-skill.sh` | `--out` 값 누락 시 무한 루프(CPU 스핀 재현) | 검증자 확인 high | 값 가드 → exit 1 |
| 2 | scripts | `test/run-evals.sh` | `--skill`/`--model` 값 누락 시 무한 루프 | 검증자 확인 medium(원 high, 수동 도구) | 값 가드 |
| 3 | scripts | `bin/package-skill.sh` | mktemp 실패 미확인 → root 면 `/` 에 쓰고 무결성 검증을 우회한 zip 을 성공으로 보고 | 검증자 확인 high | 실패 시 RC=3·해당 스킬 건너뜀 |
| 4 | scripts | `test/run-evals.sh` | 격리 HOME mktemp 실패 미확인 → `HOME=/` 로 install·auto 모드 실행 | 검증자 확인 high | 실패 시 FAIL 처리 |
| 5 | scripts | `bin/jobstack-cron` | crontab `%`·plist `&`/`<` 미이스케이프(등록은 성공, 실행 시 깨짐) | 검증자 확인 medium | `%` 이스케이프, plist 는 `xml_escape`, 테스트 (i) |
| 6 | scripts | `test/run-evals.sh` | `mapfile` 이 macOS bash 3.2 에 없음(dry-run 도 실패) | 검증자 확인 medium | while-read 루프 |
| 7 | scripts | `bin/jobstack-cron` | python3 부재 시 "새 공고 0건" 으로 성공 보고 | 검증자 확인 medium | 시작 시 `command -v python3`, 없으면 exit 1 |
| 8 | scripts | `test/run-evals.sh` | `claude -p` 세 호출에 wall-clock 타임아웃 없음 | 검증자 확인 medium | `timeout`(`EVAL_TIMEOUT_S`, 기본 900초) 래핑, 리포트에 timeout 표시 |
| 9 | scripts | `test/test-cron.sh` | status/uninstall·launchd 경로 미테스트, uname 오버라이드 훅 없음 | 검증자 확인 medium | `JOBSTACK_CRON_OS_NAME` 훅, (g) status·(h) uninstall·(i) Darwin plist 케이스 |
| 10 | scripts | `test/test-learn.sh` | `--skill` 형식·`--session` 길이 미테스트 | 검증자 확인 low | 케이스 추가 |
| 11 | scripts | `bin/jobstack-learn` | dup 판정 read-check-append 레이스 | 검증자 확인 low | `fcntl.flock`(없는 플랫폼은 그대로 진행) |
| 12 | wiring | `docs/telemetry-events.md` | 초안 통합 지시문·부록이 남은 채 스킬 2개가 이미 `jobstack-learn add` 호출 | 검증자 확인 high | 초안 헤더·지시문·부록 제거, 정식 절로 정리 |
| 13 | wiring | `company-research/references/sources.md` | ⑥ AI 사용 허용 여부가 Phase 1 수집 목록(5개)에 없음 | 검증자 확인 medium(원 high) | 전형 방식 6개 항목으로 확장, report-template 근거 표기 통일 |
| 14 | wiring | `templates/policy-checklist.md` | jobstack-cron 분기 재확인 알림이 미구현인데 사실처럼 서술 | 검증자 확인 medium(원 high) | "계획 — 아직 자동화되지 않음" 으로 완화 |
| 15 | wiring | docs/evals.md·routines.md·telemetry-events.md·`bin/jobstack-learn` | 없는 NOTES.md·telemetry-events-learnings.md 참조(#22 와 중복) | 검증자 확인 medium | 실제 위치로 교체 |
| 16 | wiring | `auto/SKILL.md` | nudge 는 항상 출력인데 조건부처럼 서술 | 검증자 확인 medium | "항상 표시" 명시 |
| 17 | wiring | `mock-interview/references/modes/tech.md` | E-2 삽입이 E 절 불릿을 가름 | 검증자 확인 low | 불릿 순서 복원 |
| 18 | wiring | `CLAUDE.md` | 프론트매터 키 목록에 model/effort 누락 | 검증자 확인 low | 추가 |
| 19 | claims | `docs/routines.md` | 비용을 "API 사용량 과금·구독과 별개" 로 단정 — 공식 문서는 구독 사용량 소모·일일 상한(검토 보고서 §2-2 는 Managed Agents 행을 오인용) | 검증자 확인 high | 정정, 출처(code.claude.com/docs/en/routines)·기준일 병기 |
| 20 | claims | `tracker/SKILL.md` | 탈락 후 체크리스트의 14일·30명 수치가 무출처 단정, `RE_NUMBER` 가 못 잡음 | 검증자 확인 medium(원 high) | 수치 제거·실행 시 WebSearch 확인. `RE_NUMBER` 에 `N일·N명` 추가는 D-7·7일 정체 같은 정상 표현과 겹쳐 채택하지 않음 |
| 21 | claims | `docs/evals.md`·`test/run-evals.sh` | 채점 모델 haiku 고정, 은퇴 하한(2026-10-15) 미언급 | 검증자 확인 medium(원 high) | `--judge-model`/`EVAL_JUDGE_MODEL`, 재확인 각주 |
| 22 | claims | docs 3곳 | NOTES.md 참조 | #15 와 중복(검증자 확인 medium) | #15 |
| 23 | claims | `docs/cowork.md` | 스킬 로드 방식 전제가 출처 없이 단정 | 검증자 기각 — 근거가 검토 보고서 §2-1(`[사실]`, 출처 공식 skills 문서)에 있음 | `[2차]` 표기 대신 §2-1 출처·기준일을 병기, 실제 세션 미검증 표기는 유지 |
| 24 | claims | `docs/cowork.md` | `/resume` 이 내장 명령과 충돌할 수 있음 | 검증자 확인 medium | 확인 항목 추가 |
| 25 | claims | `docs/routines.md` | 최소 간격 1시간을 영구 사실처럼 서술 | 검증자 확인 low(원 medium) | 기준일·연구 미리보기 변경 가능 병기 |
| 26 | claims | `templates/chrome-path.md` | 확장의 승인 팝업 미언급 | 검증자 확인 low | 한 줄 추가 |

수정은 Sonnet 5 에이전트 2개(스크립트 A/B)가 격리 복사본에서 재현→수정→테스트로 적용하고 Fable 5.1 이 통합·재검토했다. 수정 후 lint-conventions·통합 93/93·Node 테스트 7종·셸 테스트 13종·eval dry-run 15건 통과. 검증 팬아웃 완료(검증자 26, 총 30 에이전트·약 101분): real 25·기각 1(#23). 등급 조정은 high→medium 5건(#2·#13·#14·#20·#21), medium→low 1건(#25).

---

## PR #17 리뷰 반영 (2026-09-05)

저장소 소유자가 macOS 로컬(Node 25, Python 3.9)에서 재현해 남긴 심층 코드리뷰 8건. 이 환경(Linux)에서는 심링크 경로와 병렬 실행으로 같은 조건을 만들어 먼저 재현한 뒤 고쳤다.

| # | 지적 | 판정 | 조치 |
|---|---|---|---|
| 1 | `md2docx.mjs` 가 심링크 경로(macOS `/tmp` → `/private/tmp`)에서 CLI main 을 건너뛰어 산출물 없이 exit 0 | 재현(Linux 심링크 bin 경로) — 확인 high | 진입 판정을 양쪽 realpath 비교로, `jobstack-export` 는 산출물 존재·zip 검증 후에만 성공 보고(아니면 exit 3), 심링크 실행·no-op 변환기 회귀 테스트 |
| 2 | macOS 에서 preamble·cron·통합 테스트가 논리/물리 경로·launchd 기대값 때문에 실패 | 확인 medium | 테스트 기대값을 실경로(`cd -P`)로, cron 테스트의 Linux 기대 케이스에 OS 고정, CI 에 macOS smoke job(시스템 bash 3.2) |
| 3 | `run-integration-test.sh` 가 기본으로 실제 `~/.claude/skills`·`~/.jobstack` 을 수정 | 확인 high | 기본 격리 HOME(mktemp, 종료 시 삭제), 실제 설치 검증은 `--real-home` 옵트인, CONTRIBUTING 갱신 |
| 4 | `.hwp` 변환이 기본값으로 `npx -y kordoc` 원격 최신 패키지를 실행 | 확인 high | `JOBSTACK_ALLOW_NPX` 기본 0, 허용 시 `kordoc@4.12.3` 고정(`JOBSTACK_KORDOC_SPEC`), exit 3 안내에 옵트인 방법, 가짜 npx 테스트 |
| 5 | cron 에서 node/PATH 부재가 "새 공고 0건" 성공처럼 보임 | 확인 high | node 사전 확인(exit 1), 전 플랫폼 실패 exit 2 + cron.log `degraded`, install 이 crontab `PATH=`·plist EnvironmentVariables 에 node·python3 디렉토리 등록, `JOBSTACK_NODE_BIN`, 테스트 (m)(n) |
| 6 | tracker·exp 동시 add 시 lost update | 재현(구버전 tracker 병렬 20건 → 10건) — 확인 medium | tracker 는 flock, exp·defense-map 은 `bin/lib/lockfile.mjs`(O_EXCL 잠금·stale 정리·exit 훅), 병렬 20건 회귀 테스트 |
| 7 | `fetch-jobs` limit 미검증(-5·0·1e+21 이 그대로 API count 로) | 확인 medium | 1~100 정수만 허용하고 그 외 exit 1, cron `--limit` 도 같은 검증, 테스트 |
| 8 | PR 본문이 CI 결정적 테스트·eval·플러그인/Cowork 검증 범위를 구분하지 않음 | 확인 | PR 본문 "검증" 절을 범위별로 분리, `claude plugin validate .` 로컬 통과를 명시 |
| 후속 | README 플러그인 예시가 `/jobstack:job_search` 인데 플러그인 스킬 이름은 `job-search` | 확인 low — 로컬 `install.sh` 는 언더스코어 alias 를 만들지만 플러그인 네임스페이스에는 alias 가 없음(격리 HOME 의 init 이벤트 slash_commands 로 확인) | `/jobstack:job-search` 로 정정하고 네임스페이스는 하이픈·로컬 alias 와 봇은 언더스코어임을 README 에 명시 |

---

## 로컬 스킬 실측 스모크 (2026-09-06)

머지 전 마지막 확인으로 16개 스킬을 격리 HOME 에 심링크 설치해 `claude -p`(sonnet, 8턴, 300초)로 스킬마다 1케이스씩 돌렸다.
결과 표·관찰·환경 한계는 `docs/E2E-TEST-REPORT.md` 의 2026-09-06 절.

| 항목 | 결과 |
|---|---|
| 1차 통과 | 13/16 — cover_letter·review 는 8턴 초과, company_research 는 300초 초과 |
| 재실행 | cover_letter·review 는 16턴 예산에서 통과. company_research 는 10턴·480초에서 보고서 파일까지 생성하고 최종 요약 전 턴 소진(부분 통과) |
| 스크립트 배선 | tracker·exp·retro-stats·defense-map·fetch-jobs·fetch-diag·learn 호출 확인, 상태 파일 손 편집 없음 |
| 비용 | 약 6.5 USD — 리서치 스킬(salary 1.06, company_research 1.66)이 대부분 |

반영한 변경: company-research·salary·strategy 의 병렬 리서치 문구에 "Agent 호출은 한 응답에 함께 발행하고 결과를 기다린다"
를 명시했다. 1차 실행에서 서브에이전트가 백그라운드로 떠 헤드리스 턴이 결과 수신 전에 끝나고 깨움으로 이어졌기 때문이다
(재실행에서는 전경 호출로 한 턴에 수신 — 1회 관찰). 후속 후보: company-research 의 Phase 2 references 일괄 Read, eval
케이스의 cover-letter·review `max_turns` 12 이상, fetch-jobs 의 브라우저 실행 파일 경로 오버라이드(제한 환경용).
